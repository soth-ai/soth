[package]
name = "soth-node"
description = "Node.js binding for the SOTH SDK (napi-rs)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
# `http-telemetry` enables the background shipper; native Node
# bindings always ship with it on so customers' configured
# `telemetry_endpoint` actually reaches soth-cloud.
soth-sdk-core = { path = "../../crates/soth-sdk-core", features = ["http-telemetry"] }
soth-core = { workspace = true }
zeroize = { workspace = true }
napi = { version = "2", default-features = false, features = ["napi8", "serde-json"] }
napi-derive = "2"
serde = { workspace = true }
serde_json = { workspace = true }

[build-dependencies]
napi-build = "2"
