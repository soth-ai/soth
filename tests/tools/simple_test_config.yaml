# Simple test config using mock MCP server directly
version: "1.0"

server:
  transport: "stdio"

upstream:
  command: "./tests/tools/mock_mcp_server/target/debug/mock-mcp-server"
  args: []

observe:
  enabled: false

policy:
  enabled: false

budget:
  enabled: false
