[package]
name = "mock-mcp-server"
version = "0.1.0"
edition = "2021"
description = "Mock MCP server for testing SOTH"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
