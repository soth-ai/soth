[package]
name = "soth-core"
description = "Rebuilt SOTH core crate (bootstrap stage)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
thiserror = { workspace = true }
