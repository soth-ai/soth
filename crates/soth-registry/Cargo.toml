[package]
name = "soth-registry"
description = "Structured provider/app/model registry for SOTH detection"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
